pipeline { 
    agent any  
    stages { 
        stage('Init') { 
            steps { 
               echo 'This is a minimal pipeline.' 
            }
        }
        stage ('Build') {
      steps {
            sh 'mvn clean package'
          }
        }
    }
}
